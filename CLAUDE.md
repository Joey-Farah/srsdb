# rslp — working agreement

See `CONTEXT.md` (mission, tutor role, domain) and `PROGRESS.md` (roadmap, current step).

## Response style
- Default to **succinct, brief, to-the-point** responses. No verbose, fluffy, or
  over-detailed answers unless Joey explicitly asks for depth ("explain in detail",
  "go deep", etc.).
- Lead with the answer. Cut preamble. Tables/short lists over long prose.

## Plan discipline
- **Stick to the agreed roadmap** in `PROGRESS.md` — build bottom-up (storage → B+tree →
  operators → SQL), one phase at a time, no jumping ahead. Table/schema design is the
  *top* of the stack (Phase 2-as-schema), not the start.
- Tutor, not code generator: Joey writes the learning core. See `CONTEXT.md`.
